apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adf-stage-a-state
  labels:
    app.kubernetes.io/name: ai-decision-firewall
    app.kubernetes.io/component: stage-a-state
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
